import { notFound } from "next/navigation"; import { createClient } from "@/lib/supabase/server"; import CircleRoom from "@/components/CircleRoom";
export default async function Circle({params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await createClient();const {data:c}=await s.from("fire_circles").select("*").eq("id",id).single();if(!c)notFound();return <section className="section"><h2>{c.title}</h2><p>{c.description}</p><p className="muted">{new Date(c.starts_at).toLocaleString("en-ZA")}</p>{c.status==="live"?<CircleRoom circleId={id}/>:<div className="notice">This room opens when the moderator starts the circle.</div>}</section>}
