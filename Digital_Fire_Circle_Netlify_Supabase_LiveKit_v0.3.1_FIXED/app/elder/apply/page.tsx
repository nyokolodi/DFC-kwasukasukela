import { redirect } from "next/navigation"; import { requireUser } from "@/lib/auth";
async function apply(formData:FormData){"use server";const {supabase,user}=await requireUser();const payload={user_id:user.id,display_name:String(formData.get("display_name")),phone:String(formData.get("phone")),languages:String(formData.get("languages")).split(",").map(x=>x.trim()).filter(Boolean),community:String(formData.get("community")),biography:String(formData.get("biography")),status:"submitted"};const {error}=await supabase.from("storyteller_applications").upsert(payload,{onConflict:"user_id"});if(error)throw error;redirect("/elder")}
export default function Apply(){return <section className="section"><h2>Become a DFC storyteller</h2><p className="muted">You are joining as a cultural contributor, not an influencer.</p><form className="form" action={apply}><label>Name used publicly<input name="display_name" required/></label><label>Phone<input name="phone" required/></label><label>Languages, separated by commas<input name="languages" required placeholder="isiZulu, English"/></label><label>Community or region<input name="community" required/></label><label>Tell us about the stories you carry<textarea name="biography" required/></label><button className="button">Submit application</button></form></section>}
