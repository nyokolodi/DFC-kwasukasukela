import Link from'next/link';import{createClient}from'@/lib/supabase/server';
export default async function Circles(){const db=await createClient();const{data}=await db.from('fire_circles').select('id,title,description,starts_at,capacity,status').in('status',['scheduled','live']).order('starts_at');return <section className="section shell"><div className="eyebrow">Live Digital Fire Circles</div><h1>Gather, listen, participate.</h1><p className="lead">Moderated audio sessions led by vetted storytellers. No toxic chat. No algorithm chaos.</p><div className="grid">{(data??[]).map((x:any)=><article className="card" key={x.id}><span className="chip">{x.status}</span><h2 style={{fontSize:24}}>{x.title}</h2><p className="muted">{x.description}</p><p className="small">{new Date(x.starts_at).toLocaleString()} · Capacity {x.capacity}</p><Link className="btn" href={'/circles/'+x.id}>Open circle</Link></article>)}</div></section>}