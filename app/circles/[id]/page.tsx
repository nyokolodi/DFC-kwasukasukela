import{notFound}from'next/navigation';import{createClient}from'@/lib/supabase/server';import CircleRoom from'@/components/CircleRoom';
export default async function Circle({params}:{params:Promise<{id:string}>}){const{id}=await params;const db=await createClient();const{data}=await db.from('fire_circles').select('*').eq('id',id).maybeSingle();if(!data)notFound();return <section className="section shell"><div className="card"><div className="eyebrow">Digital Fire Circle</div><h1>{data.title}</h1><p className="lead">{data.description}</p><p className="muted">{new Date(data.starts_at).toLocaleString()}</p><CircleRoom circleId={data.id}/></div></section>}