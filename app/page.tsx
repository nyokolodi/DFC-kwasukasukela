import Link from'next/link';import{createClient}from'@/lib/supabase/server';
export default async function Home(){const db=await createClient();const[{data:stories},{data:circles}]=await Promise.all([db.from('story_catalog').select('*').limit(6),db.from('fire_circles').select('id,title,description,starts_at,status').in('status',['scheduled','live']).order('starts_at').limit(3)]);return <><section className="hero shell"><div><div className="eyebrow">A digital fireside for South Africa</div><h1>The fire is digital. The tradition is real.</h1><p className="lead">Digital Fire Circle reconnects children with reading, elders with purpose, and families with culture through real voices, real stories and respectful technology.</p><div className="heroActions"><Link className="btn" href="/stories">Explore stories</Link><Link className="btn secondary" href="/circles">Join a Fire Circle</Link></div></div><div className="firecard"><div><div className="eyebrow" style={{color:'#f7d5a2'}}>Tonight around the fire</div><h2>Listen. Learn. Belong.</h2><p>Folktales, history, lullabies, language stories and wisdom carried by living storytellers.</p></div></div></section><section className="section shell"><div className="row" style={{justifyContent:'space-between',alignItems:'end'}}><div><div className="eyebrow">Story library</div><h2>Stories worth staying for.</h2></div><Link href="/stories">View library →</Link></div><div className="grid">{(stories??[]).map((x:any)=><article className="card" key={x.id}><div className="chips"><span className="chip">{x.language_name}</span>{x.culture_name&&<span className="chip">{x.culture_name}</span>}</div><h3>{x.title}</h3><p className="muted">{x.summary}</p><Link href={'/stories/'+x.slug}>Open story →</Link></article>)}</div></section><section className="section shell"><div className="eyebrow">Live circles</div><h2>Gather around the fire.</h2><div className="grid">{(circles??[]).map((x:any)=><article className="card" key={x.id}><div className="chip">{x.status}</div><h3>{x.title}</h3><p className="muted">{x.description}</p><Link href={'/circles/'+x.id}>Enter circle →</Link></article>)}</div></section></>}
