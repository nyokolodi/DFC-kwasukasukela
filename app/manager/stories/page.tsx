import{redirect}from'next/navigation';import{requireRole}from'@/lib/auth';import{createClient}from'@/lib/supabase/server';
export default async function StoryOps(){const s=await requireRole(['platform_admin','moderator','content_manager']);if(!s)redirect('/manager');const db=await createClient();const{data}=await db.from('stories').select('id,title,slug,status,access_level,created_at').order('created_at',{ascending:false});async function action(form:FormData){'use server';const db=await createClient();const id=String(form.get('id'));const actionName=String(form.get('action'));if(actionName==='publish'){const{data:asset}=await db.from('story_audio_assets').select('id').eq('story_id',id).eq('variant','standard').eq('approved',true).maybeSingle();if(!asset)return;if(s.profile.role==='platform_admin'||s.profile.role==='content_manager')await db.from('stories').update({status:'published',published_at:new Date().toISOString()}).eq('id',id)}if(actionName==='approve_audio'){if(s.profile.role==='platform_admin'||s.profile.role==='content_manager')await db.from('story_audio_assets').update({approved:true}).eq('story_id',id)}}return <section className="section shell"><div className="row" style={{justifyContent:'space-between',alignItems:'center'}}><div><h1>Story operations</h1><p className="lead">Review, approve audio and publish culturally responsible archive content.</p></div><a className="btn" href="/manager/stories/new">New story</a></div><div className="grid">{(data??[]).map((x:any)=><article className="card" key={x.id}><span className="chip">{x.status}</span><h2 style={{fontSize:24}}>{x.title}</h2><p className="small muted">{x.slug}</p><p>Access: {x.access_level}</p><form action={action} className="row"><input type="hidden" name="id" value={x.id}/><button className="btn secondary" name="action" value="approve_audio">Approve audio</button><button className="btn" name="action" value="publish">Publish</button></form></article>)}</div></section>