import{redirect}from'next/navigation';import{requireRole}from'@/lib/auth';import{createClient}from'@/lib/supabase/server';
export default async function StoryOps(){const s=await requireRole(['platform_admin','moderator','content_manager']);if(!s)redirect('/manager');const db=await createClient();const{data}=await db.from('stories').select('id,title,slug,status,access_level,created_at').order('created_at',{ascending:false});return <section className="section shell"><h1>Story operations</h1><p className="lead">Human review is part of the moat. Rights, provenance, publication status and cultural restrictions remain first-class metadata.</p><div className="grid">{(data??[]).map((x:any)=><article className="card" key={x.id}><span className="chip">{x.status}</span><h2 style={{fontSize:24}}>{x.title}</h2><p className="small muted">{x.slug}</p><p>Access: {x.access_level}</p></article>)}</div></section>}