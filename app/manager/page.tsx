import Link from'next/link';import{requireStaff}from'@/lib/auth';import{createClient}from'@/lib/supabase/server';
export default async function Manager(){const s=await requireStaff();if(!s)return <section className="section shell"><div className="card"><h1>Admin access required</h1><p>Sign in with your platform administrator account.</p><Link className="btn" href="/login">Sign in</Link></div></section>;const db=await createClient();const [{count:profiles},{count:stories},{count:applications},{count:circles}]=await Promise.all([db.from('profiles').select('*',{count:'exact',head:true}),db.from('stories').select('*',{count:'exact',head:true}),db.from('storyteller_applications').select('*',{count:'exact',head:true}),db.from('fire_circles').select('*',{count:'exact',head:true})]);return <section className="section shell"><div className="eyebrow">Platform command center</div><h1>DFC Admin</h1><p className="lead">Manage the archive, storytellers, families, Fire Circles, moderation and platform operations.</p><div className="grid">{[['Profiles',profiles],['Stories',stories],['Applications',applications],['Fire Circles',circles]].map(([label,n])=><div className="card" key={label as string}><div className="small muted">{label}</div><div className="metric">{n??0}</div></div>)}</div><div className="adminnav"><Link className="btn secondary" href="/manager/stories">Stories</Link><Link className="btn secondary" href="/manager/storytellers">Storytellers</Link><Link className="btn secondary" href="/manager/circles">Fire Circles</Link></div></section>}