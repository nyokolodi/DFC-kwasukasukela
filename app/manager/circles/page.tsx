import{redirect}from'next/navigation';import{requireRole}from'@/lib/auth';import{createClient}from'@/lib/supabase/server';
export default async function CircleOps(){const s=await requireRole(['platform_admin','moderator','content_manager']);if(!s)redirect('/manager');const db=await createClient();const{data}=await db.from('fire_circles').select('id,title,starts_at,status,capacity,recording_allowed').order('starts_at',{ascending:false});return <section className="section shell"><h1>Fire Circle operations</h1><div className="grid">{(data??[]).map((x:any)=><article className="card" key={x.id}><span className="chip">{x.status}</span><h2 style={{fontSize:24}}>{x.title}</h2><p className="muted">{new Date(x.starts_at).toLocaleString()}</p><p>Capacity {x.capacity} · Recording {x.recording_allowed?'allowed':'off'}</p></article>)}</div></section>}