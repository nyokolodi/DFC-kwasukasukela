import{notFound}from'next/navigation';import{createClient}from'@/lib/supabase/server';import StoryPlayer from'@/components/StoryPlayer';
export default async function Story({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const db=await createClient();const{data:story}=await db.from('story_catalog').select('*').eq('slug',slug).maybeSingle();if(!story)notFound();return <section className="section shell"><article className="card"><div className="chips"><span className="chip">{story.language_name}</span>{story.culture_name&&<span className="chip">{story.culture_name}</span>}</div><h1>{story.title}</h1><p className="lead">{story.summary}</p>{story.audio_path?<StoryPlayer storyId={story.id}/>:<div className="status">Audio recording is being prepared.</div>}<div className="section" style={{padding:'22px 0'}}><div className="row"><span className="chip">Ages {story.age_min}–{story.age_max}</span><span className="chip">Access: {story.access_level}</span>{story.storyteller_name&&<span className="chip">Told by {story.storyteller_name}</span>}</div></div><div className="card" style={{background:'#fff7ee'}}><strong>Wisdom carried by this story</strong><p>{story.moral_lesson||'Stories carry knowledge across generations.'}</p></div></article></section>