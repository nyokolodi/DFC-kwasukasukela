import Link from'next/link';import{createClient}from'@/lib/supabase/server';
export default async function Stories(){const db=await createClient();const{data,error}=await db.from('story_catalog').select('*').order('published_at',{ascending:false});return <section className="section shell"><div className="eyebrow">African Story Library</div><h1>Stories worth staying for.</h1><p className="lead">Browse real voices, languages, histories, moral lessons and lullabies.</p>{error&&<div className="status">Library is not connected yet. Run the DFC Supabase schema.</div>}<div className="grid">{(data??[]).map((x:any)=><article className="card" key={x.id}><div className="chips"><span className="chip">{x.language_name}</span>{x.culture_name&&<span className="chip">{x.culture_name}</span>}</div><h2 style={{fontSize:24}}>{x.title}</h2><p className="muted">{x.summary}</p><p className="small muted">Ages {x.age_min}–{x.age_max}</p><Link className="btn secondary" href={'/stories/'+x.slug}>Open story</Link></article>)}</div></section>