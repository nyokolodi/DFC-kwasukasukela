import Link from'next/link';import{getSessionProfile}from'@/lib/auth';import{createClient}from'@/lib/supabase/server';
export default async function School(){const s=await getSessionProfile();if(!s||!['teacher','school_admin','platform_admin'].includes(s.profile.role))return <section className="section shell"><div className="card"><h1>School portal</h1><p>School access is reserved for licensed teachers and school administrators.</p><Link className="btn" href="/login">Sign in</Link></div></section>;const db=await createClient();const{data:schools}=await db.from('school_memberships').select('school_id,member_role,schools(id,name)').eq('user_id',s.profile.id).eq('active',true);return <section className="section shell"><div className="eyebrow">School & ECD</div><h1>Learning circles beyond the classroom.</h1><div className="grid">{(schools??[]).map((x:any)=><div className="card" key={x.school_id}><h2>{x.schools?.name||'School'}</h2><p className="muted">Role: {x.member_role}</p><p>Use licensed story collections and Fire Circles for guided listening.</p></div>)}</div></section>}