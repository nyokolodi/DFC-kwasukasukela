import{redirect}from'next/navigation';import{getSessionProfile}from'@/lib/auth';import{createClient}from'@/lib/supabase/server';
export default async function NewChild(){const s=await getSessionProfile();if(!s)redirect('/login');async function addChild(form:FormData){'use server';const db=await createClient();const{data:family}=await db.from('families').select('id').eq('owner_id',s.profile.id).maybeSingle();if(!family)redirect('/family');await db.from('child_profiles').insert({family_id:family.id,display_name:String(form.get('display_name')),birth_year:Number(form.get('birth_year'))||null,preferred_languages:String(form.get('languages')||'').split(',').map(v=>v.trim()).filter(Boolean)});redirect('/family')}return <section className="section shell"><div className="card"><div className="eyebrow">Family</div><h1>Add a child</h1><form className="form" action={addChild}><input className="input" name="display_name" placeholder="Child name or nickname" required/><input className="input" name="birth_year" type="number" placeholder="Birth year"/><input className="input" name="languages" placeholder="isiZulu, English"/><button className="btn">Save child</button></form></div></section>}