import {createClient} from '@/lib/supabase/server';import type{Role}from'./types';export async function getSessionProfile(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return null;const{data:profile}=await supabase.from('profiles').select('id,display_name,phone,role').eq('id',user.id).maybeSingle();return profile?{user,profile:profile as {id:string;display_name:string|null;phone:string|null;role:Role}}:null}export async function requireRole(roles:Role[]){const s=await getSessionProfile();if(!s||!roles.includes(s.profile.role))return null;return s}
